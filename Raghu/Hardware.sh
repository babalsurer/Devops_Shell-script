#!/bin/bash
read -p " What is your Name Please :" name
memory=$(free -m)
os=$(uname -o)
disk=$(df -h)

echo "========================================================================= Welcom $name =========================================================================="
echo "========================================================================= Memory Details ========================================================================"
echo "$memmory"
echo "=========================================================================  OS Deatials =========================================================================="
echo "$os"
echo "=========================================================================  Disk ditails ========================================================================="echo  "$disk"
echo "========================================================================= Thank you $name ======================================================================="
